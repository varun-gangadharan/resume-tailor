[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "resume-tailor"
version = "0.1.0"
description = "Local LaTeX SWE resume keyword tailor"
readme = "README.md"
requires-python = ">=3.11"

[project.scripts]
resume-tailor = "resume_tailor.cli:main"
